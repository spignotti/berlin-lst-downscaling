"""Dynamic scene data — per-scene ERA5-Land and shadow products.

Reads the v3 manifest bundle, resolves static geometry artifacts,
and produces scene-keyed dynamic products under ``ard/dynamic/``.
"""

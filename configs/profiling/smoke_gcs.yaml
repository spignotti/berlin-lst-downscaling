defaults:
  - _base
  - _self_

# GCS smoke profiling — bounded run against published GCS data.
# One asset per source/partition for quick iteration.
manifest_uri: "gs://berlin-lst-data/manifests/v3/2017-2026-cutoff-20260717T235959Z-r2/manifest.parquet"
ard_ledger_uri: "gs://berlin-lst-data/ard/full/2017-2026-cutoff-20260717T235959Z/ledger.parquet"
dynamic_root: "gs://berlin-lst-data/dynamic/full"
inference_root: "gs://berlin-lst-data/dynamic/inference/2026"
static_sources_root: "gs://berlin-lst-data/static/sources/full"
static_derived_root: "gs://berlin-lst-data/static/derived/full"
output_root: "gs://berlin-lst-data/profiling/wb2c-1-smoke"

# Bounded selection: one asset per source/partition
max_assets_per_source_and_partition: 1

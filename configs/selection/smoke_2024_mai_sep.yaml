# @package _global_
# Smoke test: Mai–Sep 2024 — validates coupling logic across the full
# configured season before running the multi-year volume scan.
defaults:
  - default
  - _self_

mode: couple
years: [2024]
months: [5, 6, 7, 8, 9]
manifest_out: "data/tmp/manifest_smoke_2024.parquet"

defaults:
  - _base
  - _self_

# Smoke test: real-data local subset
source_root: "data/static/sources/smoke"
derived_root: "data/static/derived/smoke"
output_root: "data/dynamic/smoke"

# manifest_uri must be provided on command line:
#   manifest_uri=data/ard/manifests/v3/.../manifest.parquet

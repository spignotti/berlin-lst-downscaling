defaults:
  - _base
  - _self_

# Smoke test: small local subset
source_root: "data/static/sources/smoke"
derived_root: "data/static/derived/smoke"
output_root: "data/dynamic/smoke"

# Limit to a few scenes for smoke testing
# Override on CLI with: '+years=[2024]' '+manifest_uri=...'

# @package _global_

# Stage 2: Spectral indices + morphology
# Adds: DEM, slope, aspect, building height

defaults:
  - override /datamodule: berlin

tags: ["stage_2", "morphology"]

module:
  network:
    model:
      in_channels: 9

datamodule:
  feature_set:
    - lst
    - ndvi
    - ndbi
    - ndwi
    - mndwi
    - dem
    - slope
    - aspect
    - building_height

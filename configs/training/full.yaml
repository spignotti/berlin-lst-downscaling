# @package _global_
# Full training-data release: all 324 assessable anchor scenes (2017-2025)
# on the canonical 100 m grid, writing canonical products to GCS under
# training/v1/. Invoke on the VM via scripts/run_training_data_vm.sh (P3).
defaults:
  - _base
  - _self_

scene_ids: []
bbox: null
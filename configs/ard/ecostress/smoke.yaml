# @package _global_
defaults:
  - default
  - _self_

mode: smoke
scene_date: "2018-07-30"          # matches fixture granule (tile 33UUU on this date)
output_root: "data/tmp/smoke_ecostress_2018-07-30"
viz: true
ecostress:
  # raw_dir is set dynamically by nox session (smoke-ecostress) to a
  # per-run stage path like data/tmp/ecostress_stage/<run_id>/.
  # The pipeline cleans up the stage after processing.
  tile: "33UUU"                 # MGRS tile covering western Berlin (~88% overlap)

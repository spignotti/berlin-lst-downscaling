# @package _global_
# Full ARD run — Sentinel-2 only, six-band SWIR contract.
#
# Republishes exactly the 158 Sentinel-2 scenes at schema v8 (the only
# source whose schema version changed); Landsat and ECOSTRESS rows keep
# their current version and are not touched by this config.
defaults:
  - _base
  - _self_

sources: ["sentinel-2-l2a"]
mode: full
manifest_uri: null            # provided by the VM wrapper (canonical r2 bundle)
output_root: "gs://berlin-lst-data/ard/full/2017-2026-cutoff-20260717T235959Z"
